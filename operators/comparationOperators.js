//comparation operators

// == if equal do something if not, do another checks only values.
// === if qual do something if not, do another check values and data types.
// != if not equal is True , if equal is False - only checks values
// !== if not equal is True , if equal is False - checks both values and data types
// > is the value higher ? - boolean
// < is the value lower ? - boolean
// >= is the value higher or equal? - boolean
// <= is the value lower or equal? - boolean

const a = 10;
const b = 20;
const c = '30';

a == b;